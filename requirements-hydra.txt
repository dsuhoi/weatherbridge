# Hydra entry-point dependencies (Phase 2 of the refactor).
# Install on top of the existing wti-train:v1 docker image:
#   pip install -r requirements-hydra.txt
hydra-core==1.3.2
omegaconf>=2.3,<2.4
