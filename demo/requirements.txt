streamlit>=1.30
numpy>=1.24
matplotlib>=3.7
cartopy>=0.22
# The precompute step additionally needs torch + xarray + pytorch_lightning + the
# project source tree (~/dsuhoi/weather_time_interpolation). Those are already
# installed in the `ai_scientist` env on cloudru.
xarray>=2023.10
