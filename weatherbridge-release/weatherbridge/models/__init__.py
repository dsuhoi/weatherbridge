"""Vendored model definitions.

Deliberately empty: importing this package must not pull in every
architecture, so each module is imported by name on demand. The files are
copied verbatim from the research repository; only their imports differ.
"""
